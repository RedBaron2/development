# Test against this version of Node.js
environment:
  nodejs_version: "0.10"

# Install scripts. (runs after repo cloning)
install:
  # Get the latest stable version of Node.js or io.js
  #- ps: Install-Product node $env:nodejs_version
  # install modules
  - cinst chocolatey -y
  - cinst au -y

# Post-install test scripts.
test_script:

#- ps: .\test\testing.ps1
- ps: .\player3.ps1
- ps: cd automatic\flashplayeractivex
- ps: .\update.ps1
- ps: cd automatic\flashplayerplugin
- ps: .\update.ps1
- ps: cd automatic\flashplayerppapi
- ps: .\update.ps1

  # Output useful info for debugging.
  #- node --version
  #- npm --version
  # run tests
  #- npm test

# Don't actually build.
build: off
